pipeline {
    agent any

    stages {
        stage('‌Build') {
            steps {
                echo 'started'
                sh 'gcc --version'
            }
        }
        stage('Test'){
            sh 'printf("YYEEESSS")'
        }
    }
}
